apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: cloudserve
  name: sandbox-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sandbox
  template:
    metadata:
      labels:
        app: sandbox
    spec:
      containers:
        - name: sandbox
          image: ghcr.io/venkatakrishna8730/cloudserve-sandbox:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5002
          envFrom:
            - configMapRef:
                name: config
            - secretRef:
                name: secrets
---
apiVersion: v1
kind: Service
metadata:
  name: sandbox-service
spec:
  selector:
    app: sandbox
  ports:
    - port: 5002
      targetPort: 5002
